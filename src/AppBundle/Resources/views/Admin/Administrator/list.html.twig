{% extends 'AppBundle::navbarBO.html.twig' %}

{% block body %}
    <div class="admin index page-wrap">
        <div class="wc-title-banner">
            <h2>Les des administrateurs</h2>
        </div>

        <div class="wc-container">
            <div class="col-sm-12">
                <div class="wc-admin-block">
                    <div class="wc-admin-row">
                        <div class="row">
                            <table class="table table-striped table-list-admininistrators">


                                <thead>
                                    <th>Id</th>
                                    <th>Nom</th>
                                    <th>Prenom</th>
                                    <th>Email</th>
                                </thead>
                                <tbody>
                                    {% for administrator in administrators %}
                                        <tr>
                                            <td><a href="{{ path('admin.administrator.show', {'administratorId': administrator.id}) }}">{{ administrator.id }}</a></td>
                                            <td>{{ administrator.identity.lastName }}</td>
                                            <td>{{ administrator.identity.firstName }}</td>
                                            <td>{{ administrator.identity.email }}</td>

                                            <td>
                                                <a href="{{ path('admin.administrator.delete', {'administratorId': administrator.id}) }}"><button type="button" class="btn-admin-form btn-modify" name="button">Supprimer</button></a>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                            <div class="">
                                <a class="col-md-4" href="{{ path('admin.juror.list') }}"><button type="button" class="btn-admin-form btn-modify">Jurés</button></a>
                                <a class="col-md-4" href="{{ path('admin.administrator.list') }}"><button type="button" class="btn-admin-form btn-modify">Administrateurs</button></a>
                                <a class="col-md-4" href="{{ path('admin.campaign_administrator.list') }}"><button type="button" class="btn-admin-form btn-modify">Administrateurs de campaigne</button></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
